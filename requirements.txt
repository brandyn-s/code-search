# anthropic 1.0.0 (2026-08-20) is a declared BREAKING release (see its
# MIGRATION.md). The bound is deliberate: do not cross a breaking major on an
# unbounded spec, where a fresh `pip install` silently adopts it.
#
# Measured 2026-08-21 with `pip install --dry-run --ignore-installed`:
#   unbounded  -> anthropic==1.0.0, httpx2==2.12.0, httpx==0.28.1
# NOTE httpx2 is a SEPARATE distribution, not an httpx major upgrade -- httpx
# stays at 0.28.1, so this repo's 11 direct httpx.Client() call sites are
# unaffected. The exposure is the anthropic SDK surface itself, which we use
# narrowly (anthropic.Anthropic / anthropic.AsyncAnthropic), so the migration is
# expected to be small. Lift this bound after reading MIGRATION.md and running
# tests/unit against 1.x.
anthropic>=1.2.0,<2.0.0
click>=8.5.0
faiss-cpu>=1.15.0
httpx>=0.28.1
huggingface-hub>=1.29.0
mcp>=2.1.1,<3
numpy
PyYAML>=6.0.3
rich>=15.0.0
tree-sitter>=0.26.0
tree-sitter-c>=0.24.2
tree-sitter-c-sharp>=0.23.5
tree-sitter-cpp>=0.23.4
tree-sitter-go>=0.25.0
tree-sitter-java>=0.23.5
tree-sitter-javascript>=0.25.0
tree-sitter-markdown>=0.5.1
tree-sitter-nix>=0.1.0
tree-sitter-python>=0.25.0
tree-sitter-rust>=0.24.2
tree-sitter-svelte>=1.0.2
tree-sitter-typescript>=0.23.2
