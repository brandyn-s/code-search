# Managed by redacted-org/.github — do not edit directly
name: PR Security Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  fast-ai-scan:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
      - uses: redacted-org/pr-security-review/fast-gate@3286d7660757f0fd112287078f846a3dca96078f # v0.7.2

  analyze:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
      - uses: redacted-org/pr-security-review@3286d7660757f0fd112287078f846a3dca96078f # v0.7.2
        with:
          claude-api-key: ${{ secrets.CLAUDE_API_KEY }}
          approve-comment: "true"
          severity-threshold: "low"
          exclude-paths: "tests/fixtures/*,docs/*"
          block-mode: "high"
